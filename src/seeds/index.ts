//Due to problem with global imporst of seeders we need to pass them directly
import { CreateStudentsAndAccounts } from './CreateStudentsAndAccounts';

export const seedersClasses = [CreateStudentsAndAccounts];
